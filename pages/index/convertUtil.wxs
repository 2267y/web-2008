var filters = {
  toFix: function (value) {
    return Number(value).toFixed(2)// 保留两位小数
  }
}
var lters = {
  toAll: function (value) {
    return Number(value).toFixed(0)// 保留小数
  }
}
module.exports = {
  toFix: filters.toFix,
  toAll: lters.toAll,
}